#ifndef __PREFIX_SUM_H
#define __PREFIX_SUM_H


int prefix_sum(int array[], int length); // last must be less than the length of the array
bool non_negative_sum(int array[], int length);
bool non_positive_sum(int array[], int length);

#endif
